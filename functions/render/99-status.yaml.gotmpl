# code: language=yaml
{{ if $observedResources }}
---
apiVersion: aws.hops.ops.com.ai/v1alpha1
kind: Account
status:
  ready: {{ $accountReady }}
  accountId: "{{ $accountId }}"
  organizationAccountAccessRoleArn: "{{ $organizationAccountAccessRoleArn }}"
{{ end }}
